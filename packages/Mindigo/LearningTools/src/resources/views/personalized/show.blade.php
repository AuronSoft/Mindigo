@extends('Mindigo-dashboard::layouts')
@section('title', $set->title . ' · Auronsoft LMS')
@section('styles') @vite(['packages/Mindigo/Dashboard/src/resources/css/app.css', 'packages/Mindigo/Dashboard/src/resources/js/app.js']) @endsection
@section('content')
<div class="flex min-h-screen flex-col bg-slate-50">@include('learning-tools::partials.header', ['eyebrow' => __('learning-tools::app.personalized.title'), 'title' => $set->title, 'subtitle' => collect([$set->subject, $set->topic, $set->classroom?->name])->filter()->join(' · ')])<main class="p-6"><div class="mx-auto max-w-4xl"><div class="flex justify-end gap-2">@if(auth()->user()->role === 'student')<form method="POST" action="{{ route('learning-tools.personalized.start', $set) }}">@csrf<button class="rounded-full bg-green-600 px-6 py-3 text-sm font-black text-white">@lang('learning-tools::app.personalized.start')</button></form>@endif @if((int) $set->creator_id === (int) auth()->id())<form method="POST" action="{{ route('learning-tools.personalized.destroy', $set) }}">@csrf @method('DELETE')<button class="rounded-full border border-red-200 px-5 py-3 text-sm font-black text-red-600">@lang('learning-tools::app.actions.delete')</button></form>@endif</div><ol class="mt-5 space-y-3">@foreach($set->questions as $question)<li class="rounded-2xl border border-slate-200 bg-white p-5"><p class="text-xs font-black uppercase text-slate-400">{{ $loop->iteration }} · {{ collect([$question->subject, $question->topic, __('learning-tools::app.difficulties.' . $question->difficulty)])->filter()->join(' · ') }}</p><p class="mt-2 font-bold text-slate-800">{{ $question->content }}</p></li>@endforeach</ol></div></main></div>
@endsection
