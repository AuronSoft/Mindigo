@extends('Mindigo-dashboard::layouts')
@section('title', __('learning-tools::app.admissions.title') . ' · Auronsoft LMS')
@section('styles') @vite(['packages/Mindigo/Dashboard/src/resources/css/app.css', 'packages/Mindigo/Dashboard/src/resources/js/app.js']) @endsection
@section('content')
<div class="flex min-h-screen flex-col bg-slate-50">@include('learning-tools::partials.header', ['eyebrow' => __('learning-tools::app.eyebrow'), 'title' => __('learning-tools::app.admissions.title'), 'subtitle' => __('learning-tools::app.admissions.subtitle')])<main class="p-6">
<form method="GET" class="grid gap-3 rounded-3xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-5"><input name="keyword" value="{{ request('keyword') }}" placeholder="{{ __('learning-tools::app.admissions.search') }}" class="h-11 rounded-xl border border-slate-200 px-4 text-sm font-semibold md:col-span-2"><select name="year" class="h-11 rounded-xl border border-slate-200 px-3 text-sm font-bold"><option value="">@lang('learning-tools::app.admissions.all_years')</option>@foreach($years as $year)<option value="{{ $year }}" @selected(request('year') == $year)>{{ $year }}</option>@endforeach</select><select name="combination" class="h-11 rounded-xl border border-slate-200 px-3 text-sm font-bold"><option value="">@lang('learning-tools::app.admissions.all_combinations')</option>@foreach($combinations as $code => $subjects)<option value="{{ $code }}" @selected(request('combination') === $code)>{{ $code }}</option>@endforeach</select><button class="rounded-full bg-green-600 px-5 text-sm font-black text-white">@lang('learning-tools::app.actions.filter')</button></form>
<section class="mt-5 space-y-3">@forelse($programs as $program)<article class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-xs font-black uppercase text-green-700">{{ $program->university->code }} · {{ $program->year }}</p><h2 class="mt-1 font-black text-slate-950">{{ $program->major_name }}</h2><p class="mt-1 text-sm font-semibold text-slate-500">{{ $program->university->name }}{{ $program->major_code ? ' · '.$program->major_code : '' }}</p></div><div class="flex items-center gap-3">@if($program->benchmark_score !== null)<div class="text-right"><p class="text-[10px] font-black uppercase text-slate-400">@lang('learning-tools::app.admissions.benchmark')</p><strong class="text-2xl font-black text-green-600">{{ number_format($program->benchmark_score, 2) }}</strong></div>@endif<form method="POST" action="{{ route('learning-tools.admissions.favorite', $program) }}">@csrf<button class="grid h-10 w-10 place-items-center rounded-full border border-slate-200 {{ $favoriteIds->contains($program->id) ? 'bg-amber-50 text-amber-500' : 'text-slate-400' }}"><x-heroicon-o-star class="h-5 w-5" /></button></form></div></div><div class="mt-4 flex flex-wrap gap-2"><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">{{ $program->method }}</span>@foreach($program->combinations ?? [] as $combination)<span class="rounded-full bg-green-50 px-3 py-1 text-xs font-black text-green-700">{{ $combination }}</span>@endforeach</div>@if($program->source_url)<a href="{{ $program->source_url }}" target="_blank" rel="noopener noreferrer" class="mt-4 inline-flex text-xs font-black text-green-700 no-underline">@lang('learning-tools::app.admissions.official_source')</a>@endif</article>@empty<div class="rounded-3xl border border-dashed border-slate-300 bg-white py-16 text-center"><p class="font-black text-slate-600">@lang('learning-tools::app.admissions.empty')</p></div>@endforelse</section><div class="mt-5">{{ $programs->links() }}</div></main></div>
@endsection
