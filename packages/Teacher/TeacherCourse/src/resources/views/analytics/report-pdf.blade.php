<!doctype html><html><head><meta charset="utf-8"><title>{{ __('teacher-course::analytics.reporting') }}</title></head><body><h1>{{ __('teacher-course::analytics.reporting') }}</h1><p>{{ now()->format('d/m/Y H:i') }} · {{ __('teacher-course::analytics.scopes.'.$scope) }}</p><table width="100%" border="1" cellspacing="0" cellpadding="5"><thead><tr>@foreach($headers as $header)<th>{{ $header }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $cell)<td>{{ $cell }}</td>@endforeach</tr>@endforeach</tbody></table></body></html>
